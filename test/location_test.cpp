#include <gtest/gtest.h>
#include "../src/location.hpp"

// agents are added
TEST(LocationTest, AddAgent) {
    // add new agent

    // re-add existing agent
}

// number of agents is reported
TEST(LocationTest, AgentCount) {

}

// agents are removed
TEST(LocationTest, RemoveAgent) {
    // remove existing agent

    // try to remove non-existing agent
}

// all agents are removed
TEST(LocationTest, ClearAgents) {

}
